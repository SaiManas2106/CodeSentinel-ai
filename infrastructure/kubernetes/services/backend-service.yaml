apiVersion: v1
kind: Service
metadata:
  name: codesentinel-backend
  namespace: codesentinel
spec:
  type: ClusterIP
  selector:
    app: codesentinel-backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
